# Python Fibonacci

Here's how to compute Fibonacci numbers:

```python
def fib(n):
    if n <= 1:
        return n
    return fib(n - 1) + fib(n - 2)

print(fib(10))
```

@tip
For large n, use an iterative approach instead of recursion.
@end